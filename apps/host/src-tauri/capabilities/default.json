{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Contixis host",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default"
  ]
}
